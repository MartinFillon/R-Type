{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "name": "Entity",
    "properties": {
        "name": {
            "description": "the system name",
            "type": "string"
        },
        "active": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "wether the system should be activated"
        },
        "components": {
            "type": "object"
        }
    },
    "required": [
        "active", "name", "components"
    ]
}
