{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "name": "a system",
    "properties": {
        "name": {
            "description": "the system name",
            "type": "string"
        },
        "active": {
            "type": "boolean",
            "description": "wether the system should be activated"
        }
    },
    "required": [
        "active", "name"
    ]
}
